在Linux下使用C语言实现UDP网路编程，有必要的错误判定机制。

客户端和服务器端UDP通信功能包括：
1、收到报文，返回ACK确认消息
2、报文丢失的超时重发功能（重发三次）

客户端程序：
1、通过命令行启动，并从命令行获取三个参数：服务器IP，服务器端口，要下载的目录
2、连接服务器，请求下载服务器上某个目录的所有内容
3、接收服务器传输过来的数据，保存到本地。

服务器程序要求：
1、并发处理多个客户端的请求
2、按照客户端的请求内容，对某目录进行打包压缩，并传输给客户端。